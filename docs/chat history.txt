what flow should i fallow now:
1- do a project wide review, do bug fix for the findings, do a safe project wide refactor, update docs comments, .md and prag
2- do a safe project wide refactor, do a project wide review, do bug fix for the findings, update docs comments, .md and prag

after fallowing the flow should i do one more review + bug fix?

what about the last remaining "Edit Book" from TODO_PROMPTS.md ?
should i create it first then go with the flow or create anthor flow with this after completing the first?

when refactoring how should we handle pdf depec? spilt it from active code into a folder for it own?


I’m grounding this in the repo first so the recommendation matches the project’s actual architecture and outstanding work. I’m going to read the graph/docs guidance, the TODO prompt for “Edit Book,” and the current PDF-related code/dependencies before I recommend a flow.


Ran 5 commands
I’ve confirmed the repo already treats PDF as deprecated and outside the active shell flow, which is important for the ordering advice. I’m digging one layer deeper into the current docs and source references now so the recommendation accounts for how “Edit Book” and the parked PDF path actually sit in the codebase.


Ran 5 commands
The repo shape is clearer now: EPUB is the active path, while PDF still has a meaningful amount of runtime code and model surface hanging off the live app shell. I’m checking the dependency boundary next so I can answer whether “move it into its own folder/module” is the right kind of refactor versus just isolating it behind cleaner interfaces.


Ran 5 commands
I’ve got enough of the repo map to compare the two flows meaningfully. I’m loading the risk/test docs once more before I lock the recommendation, mainly because “Edit Book” and the parked PDF code both intersect the project’s highest-risk areas.


Ran 3 commands
The risk docs line up with what we saw in code: EPUB reading/restoration is the sacred path, and PDF is intentionally parked but still bleeds into live models, parser facade, and app-shell routing. I’m just checking the actual PDF file boundaries and dependency declarations now so I can recommend the safest isolation strategy instead of a vague “move it somewhere” answer.

Ran Get-Content app\src\main\java\com\epubreader\feature\reader\PdfReaderScreen.kt -TotalCount 220
Ran Get-Content gradle\libs.versions.toml
Ran Get-Content app\src\main\java\com\epubreader\data\parser\PdfToEpubConverter.kt -TotalCount 220
Plan



Recommended Flow: Stabilize First, Feature Second
Summary
Follow flow 1, not flow 2: project-wide review -> bug fixes -> safe refactor -> docs/comments/.md/graph.
After that, do one more targeted stabilization review + bug-fix pass, not another full from-scratch repo review.
Keep Edit Book as a separate second flow after the first flow is green.
Keep PDF disabled during the first flow.
First Flow
Start with a repo-wide review grounded in docs/project_graph.md, graphify-out/GRAPH_REPORT.md, and the area docs.
Prioritize findings in the active EPUB path first: AppNavigation, EpubParser, and ReaderScreen.
Fix correctness, crash, regression, and state-integrity issues before any structural cleanup.
Run the existing baseline tests after bug fixes so current behavior is locked before refactoring.
Do the safe refactor only after the bug-fix pass is green.
Keep the refactor behavior-preserving wherever possible: reduce coupling, clarify ownership, trim dead paths, improve naming, and tighten comments.
Update code comments, docs/*.md, and graphify artifacts at the end of the refactor pass.
Finish with one more targeted review + bug-fix pass focused on touched surfaces and core user flows.
Why Flow 1 Wins
Refactoring first would hide pre-existing bugs under structural churn and make findings harder to trust.
This repo has fragile behavior in reader restoration and parser flow, so locking down correctness before cleanup is safer.
The current PDF code is deprecated but still leaks into live shell/parser/model surfaces, which makes “refactor first” especially noisy.
Edit Book
Do not build Edit Book before the first flow.
Do not fold Edit Book into the first flow.
Create a second dedicated flow for Edit Book after stabilization/refactor is green.
Keep Edit Book EPUB-only in v1: custom cover, editable title/author, add/delete chapter, and safe progress fallback when saved chapter hrefs disappear.
Give Edit Book its own tests, docs update, and a feature-scoped regression pass after implementation.
PDF Deprecation Handling
Do not just move PDF files into a random folder; split by ownership boundary.
In the first flow, isolate deprecated PDF code into dedicated legacy packages such as data/pdf/legacy and feature/pdf/legacy.
Pull PDF-specific conversion/work/viewer orchestration out of the active EPUB path so AppNavigation and EpubParser stop importing converter/worker/viewer details directly.
Keep only a small internal bridge for legacy metadata visibility and disabled-state messaging.
Do not split PDF into a separate Gradle module in this first pass.
A module split is a later option only if PDF is coming back; for now it adds build churn without reducing risk fast enough.
Keep existing BookFormat.PDF and legacy metadata compatibility until the boundary cleanup is complete.
Do not change book ID generation or DataStore schema in this flow.
Test Plan
Before refactor, run the existing JVM baseline plus the smallest high-risk runtime checks for reader restoration, overscroll, parser integration, and parked PDF-disabled shell behavior.
After refactor, rerun assembleDebug, testDebugUnitTest, and the same targeted runtime checks to prove behavior stayed stable.
After Edit Book, add JVM coverage for metadata mutation and chapter mutation, plus focused runtime coverage only if reader restoration fallback or edit UI behavior cannot be proven locally.
Assumptions
PDF remains intentionally disabled in the active shell during the first flow.
The goal of the first flow is stabilization and boundary cleanup, not reviving PDF.
Docs updates include code comments, docs/*.md, and graph refresh.
Edit Book is still worth doing soon, but it is safer and cheaper after the first flow completes.
Implement plan


100 previous messages
I’m starting with the review/stabilization pass in the real codebase, centered on the active EPUB path and the parked PDF boundary. First I’ll inspect the current implementation and test surface in AppNavigation, EpubParser, and ReaderScreen, then I’ll turn the findings into a small, safe sequence of fixes before touching the refactor.

I’m mapping the current implementation and existing tests before I change anything so we can keep the fixes surgical. I’ll inspect the live EPUB flow, the PDF leakage points, and the current automated coverage side by side.


Ran 7 commands
The worktree is already dirty across the exact areas we need to touch, so I’m treating those edits as intentional and reading them before I change anything. I want to build on the current direction rather than accidentally undo part of the PDF-boundary cleanup that’s already in progress.


Ran 6 commands
A lot of the requested boundary work is already partially staged in the repo, which is useful. I’m reviewing the supporting model/UI/test changes now to see where the current refactor is incomplete or unsafe before I run the suite and fix what breaks.


Ran 7 commands
I’ve confirmed the repo already contains a substantial partial pass of the PDF boundary work, plus new tests and docs. I’m running the unit baseline now to find the concrete gaps instead of guessing where the refactor still isn’t safe.


Ran 2 commands
The baseline is green, which is a good sign: we’re not fixing blind breakage anymore, we’re finishing the boundary cleanup safely. I’m narrowing the remaining work to the parts that still keep PDF internals tangled into active app-shell and parser code.


Ran 4 commands
The biggest remaining coupling is now pretty clear: the shell and active reader still know about legacy PDF UI/runtime even though the shell blocks PDF. I’m inspecting the reader contracts next so I can trim that dead coupling without touching the EPUB restoration state machine.


Ran 3 commands
The unit/build baseline held, so I’m taking one pass at the highest-risk runtime checks before I refactor further. I’m targeting reader restoration, overscroll, parser integration, and the parked PDF-disabled shell behavior specifically.


Ran 1 command
The runtime check hit a Windows Gradle lockfile permission issue rather than a product failure. I’m retrying the same targeted instrumentation run with elevated permissions so we can tell whether the fragile reader/parser flows are actually still green.


Ran 2 commands
I’ve got the cleanup cut line now: keep EPUB behavior untouched, move legacy PDF runtime into dedicated packages, pull dead PDF UI out of the active reader, and isolate the shell’s remaining legacy bridge. I’m starting those file edits next.


Created 3 files, edited 5 files, ran 3 commands
The bridge layer is in place now: parser no longer depends on the converter/worker details directly, and the legacy runtime has a dedicated home. I’m sweeping the remaining references and updating the tests around those moved types before I tighten the app-shell and reader cleanup.


Edited 6 files, ran 4 commands
The structural cleanup is in: legacy PDF runtime moved out, the active reader no longer carries dead PDF fallback UI, and the shell now uses a small legacy observer instead of inlining the work-manager details. I’m compiling and running the unit suite now to catch any missed references before I touch docs and graph artifacts.


Edited 3 files, ran 4 commands
The first compile pass surfaced only a few fallout references from the moves, and those are fixed now. I’m rerunning build and unit verification to make sure the refactor boundary is actually stable before I update docs.


Ran 2 commands
The refactor now compiles; the remaining failure is a Windows file-lock issue in the Kotlin build cache during the unit run, not a code assertion failure. I’m retrying the unit suite with elevated permissions so we can confirm the code path is still green after the moves.


Ran 1 command
The code baseline is green again after the refactor. I’m rerunning the same focused runtime checks now so we have pre/post evidence on the fragile reader/parser flows and the PDF-disabled shell boundary.


Ran 1 command
The code paths are holding after the move, and the runtime checks stayed green. I’m on the documentation/graph pass now: updating the repo memory so the new legacy boundaries are reflected in the docs instead of only in code.


Edited 9 files, ran 11 commands
The docs memory is updated to match the new boundary. I’m doing one last source sweep now for stale references to the old PDF locations and then I’ll rebuild the graph artifacts so the repo map stays in sync.


Ran 3 commands
I’ve finished the functional sweep; the remaining work is repo memory and small readability polish. I’m adding brief file-level guidance to the new bridge files, then I’ll rebuild graphify so the generated map matches the refactor.


Edited 3 files, ran 1 command
The graph rebuild completed, but the staleness script returned its usual nonzero status because it detected stale inputs before rebuilding. I’m running one quick follow-up check to confirm the graph is now in sync.


Ran 3 commands
Implemented the stabilization/refactor pass. The active EPUB flow stays centered in AppNavigation and ReaderScreen, while the deprecated PDF runtime is now isolated under data/pdf/legacy and feature/pdf/legacy. I added AppNavigationPdfLegacy.kt, PdfLegacyBridge.kt, and PdfLegacyRuntime.kt so the shell and parser keep only small legacy seams, and I removed dead PDF fallback wiring from the active reader path. I also updated the docs and rebuilt the graphify artifacts so the repo memory matches the new boundary.

Verification stayed green:

.\gradlew.bat assembleDebug
.\gradlew.bat testDebugUnitTest
Targeted instrumentation for ReaderScreenRestorationTest, ReaderScreenOverscrollTest, ParserIntegrationTest, and AppNavigationPdfRepresentationFlowTest
python scripts\check_graph_staleness.py --rebuild, then python scripts\check_graph_staleness.py reported FRESH
The ignored PDF representation tests are still skipped by design, which matches the current EPUB-only shell boundary. Edit Book was intentionally not started here and remains the next separate flow after this stabilization pass.


29 files changed
+490
-432
Undo
app/src/main/java/com/epubreader/app/AppNavigationPdfLegacy.kt
app/src/main/java/com/epubreader/data/parser/PdfLegacyBridge.kt
app/src/main/java/com/epubreader/data/pdf/legacy/PdfToEpubConverter.kt
app/src/main/java/com/epubreader/data/pdf/legacy/PdfConversionWorker.kt
app/src/main/java/com/epubreader/feature/pdf/legacy/PdfReaderScreen.kt
app/src/main/java/com/epubreader/data/pdf/legacy/PdfLegacyRuntime.kt
app/src/main/java/com/epubreader/app/AppNavigationPdfLegacy.kt
app/src/main/java/com/epubreader/data/parser/EpubParser.kt
app/src/main/java/com/epubreader/app/AppNavigation.kt
app/src/main/java/com/epubreader/feature/reader/ReaderScreenContracts.kt
app/src/main/java/com/epubreader/feature/reader/ReaderScreen.kt
app/src/main/java/com/epubreader/feature/reader/ReaderScreenChrome.kt
app/src/test/java/com/epubreader/data/parser/EpubParserFacadeTest.kt
app/src/test/java/com/epubreader/data/parser/PdfToEpubConverterTest.kt
app/src/main/java/com/epubreader/app/AppNavigation.kt
app/src/main/java/com/epubreader/data/pdf/legacy/PdfToEpubConverter.kt
app/src/main/java/com/epubreader/feature/pdf/legacy/PdfReaderScreen.kt
docs/AI_DEBUG_GUIDE.md
docs/AI_ENTRY_POINTS.md
docs/PDF_review.md
docs/README.md
docs/app_shell_navigation.md
docs/epub_parsing.md
docs/package_map.md
docs/project_graph.md
docs/test_checklist.md
app/src/main/java/com/epubreader/app/AppNavigationPdfLegacy.kt
app/src/main/java/com/epubreader/data/parser/PdfLegacyBridge.kt
app/src/main/java/com/epubreader/data/pdf/legacy/PdfLegacyRuntime.kt
next?


Next should be a clean handoff into the second flow, not another repo-wide sweep.

Commit this stabilization/refactor first, do a short manual smoke pass on real app behavior, then start Edit Book as its own EPUB-only feature branch/flow. I would not do another full project-wide review now; the “one more review + bug-fix” step is already satisfied by the post-refactor verification and targeted runtime checks.

Best next sequence:

Manual smoke: import/open EPUB, reopen restores position, folder actions, settings/theme, PDF entries stay blocked.
Commit the stabilization work as its own checkpoint.
Start Edit Book v1:
custom cover
editable title/author
delete chapter
add chapter
safe fallback when saved lastChapterHref no longer exists
Ship its JVM tests first, then only add runtime coverage if restore/fallback behavior needs it.
I would keep Selectable Text after Edit Book, and keep full PDF revival after that. The PDF boundary is now safer; this is a good place to stop widening scope.

If you want, I can start the Edit Book implementation now.


push: done, manual smoke: done
go ahead and lets continue with our plan.



138 previous messages
I’m moving into the second flow now: Edit Book as a separate EPUB-only feature pass. I’ll reload the relevant docs and current app/parser boundaries first, then I’ll implement the smallest safe version with tests and keep reader restoration compatibility front and center.


Ran 8 commands
I’ve reloaded the repo memory and the worktree looks clean, so we can treat Edit Book as a fresh feature pass. I’m narrowing the current implementation surface now: where library actions live, how metadata is persisted, and what parser hooks already exist for safe EPUB mutation.


Ran 8 commands
Context automatically compacted
I’m moving into the Edit Book feature pass now and I’m tightening the scope against the existing parser and library flows before we touch code. I’ll inspect the current metadata/chapter handling, find any latent edit hooks, and then wire the smallest EPUB-only implementation that preserves reader restoration.


Ran 5 commands
The feature surface looks intentionally blank, which is good news for us: this should be a clean add instead of reviving half-finished UI. I’m loading the parser, app-shell contracts, and model files now so we can place editing logic where the architecture expects it.


Ran 6 commands
I’ve confirmed the feature is still unimplemented and the sensitive part will be progress compatibility after chapter edits. I’m asking the graph for the narrowest owning files now, then I’ll map that onto the parser/app-shell/UI seams before I start patching.


Ran 11 commands
I’ve got the current ownership map now. The cleanest path is looking like: parser owns EPUB mutation, app shell owns launching/saving/editing state, and we only touch reader logic where chapter-deletion compatibility actually forces it. I’m reading the remaining parser/chrome pieces that affect href stability and library actions before I wire the new screen.


Ran 8 commands
I’m at the implementation fork now. To keep this durable after rescan/reparse, I’m going to mutate the stored EPUB itself rather than layering fragile metadata-only overrides; I’m checking the exact epublib surface we already ship so we can do that safely instead of guessing at writer APIs.


Ran 4 commands
The library gives us enough to mutate a Book and rewrite an EPUB cleanly, so I’m going with that route. Next I’m checking the exact resource/spine helpers we’ll use for cover replacement and chapter add/delete, then I’ll patch the parser and shell together in one pass.


Ran 7 commands
Plan for this pass:

Add shared edit-book models plus parser-side EPUB mutation helpers for metadata, custom cover, and chapter add/delete.
Wire app-shell save flow and progress repair so deleted-chapter restores fall back safely.
Add an EPUB-only EditBookScreen and expose it from library cards without disturbing selection/open behavior.
Ship focused tests for parser mutation and progress repair, then refresh docs/graph and rerun verification.
I’ve got the implementation path locked now, and I’m starting the code edits in the shared model/parser layer first so the UI can sit on top of something stable. I’m adding the edit request types and the EPUB mutation helper next, then I’ll wire the shell and screen around it.


Created 2 files, edited 1 file
I’ve finished the core mutation helper and I’m wiring it into the public parser/app-shell surface now. After this step, we’ll have a stable save path with chapter-safe progress repair, then I can hang the UI on top of it without inventing extra state owners.


Edited 2 files
I’m on the UI layer now. The screen I’m adding is intentionally simple and EPUB-scoped: edit metadata, replace cover, append/delete chapters, and save through the shell-owned operation we just wired.


Created 1 file, edited 1 file
I hit a small patch mismatch in the library grid file, so I’m re-reading the exact block and then I’ll land the edit wiring cleanly instead of forcing it. After that the remaining shell work is mostly screen routing and save-state hookup.


Edited 3 files, ran 2 commands
I’m wiring the shell routing now: edit entry from library cards, save-in-flight/error state, and the new screen branch. Once this is in, we’ll have the whole feature connected end-to-end and I can shift into tests and docs.


Edited 1 file
The feature is connected enough to compile-check now, and I’d rather catch any API mismatches before I pour more time into tests/docs. I’m running a build pass next, then I’ll fix whatever the compiler surfaces and add the focused coverage on top.


Ran 1 command
The compiler’s happy, so I’m putting the guardrails in now: one pure parser test file for EPUB mutation and one pure helper test file for deleted-chapter progress repair. That should give us real confidence without dragging reader instrumentation into this pass unless we need it later.


Created 2 files, ran 1 command
The new tests found a real issue immediately, which is exactly what we want at this stage. I’m checking the failing lines now to see whether the EPUB builder fixture is invalid or the edit helper is tripping on a bad assumption, then I’ll patch it before moving on.


Edited 1 file, ran 3 commands
The failing test exposed a subtle XML issue: the generated XHTML strings had leading whitespace ahead of the XML declaration. I’m fixing that in both the production EPUB writer path and the test fixture so we don’t ship a malformed edited book.


Edited 1 file, ran 2 commands
The test caught another important behavior issue: the generated cover page was being injected into the spine, which would have changed chapter counts and restore targets. I’m correcting that now so a custom cover stays library metadata only and doesn’t become a fake readable chapter.


Edited 2 files, ran 3 commands
I’m adding one runtime flow test too, just enough to prove the shell handoff works in practice: open edit, change metadata, append a chapter, save, and see the library reflect it. That gives us feature-level confidence without dragging the reader into a broader instrumentation sweep.


Created 1 file
The feature test is in place, and I’m doing one fast compile pass on the instrumentation side before I touch docs. If this surfaces any Compose-test mismatch, I’ll fix it now while the context is still warm.


Ran 1 command
The instrumentation compile found two small, low-risk issues: a wrong Compose-test import and a suspend function reference in teardown. I’m cleaning those up now, then I’ll rerun the compile and move straight into docs plus the final verification pass.


Edited 1 file, ran 1 command
The code path is solid now, so I’m updating the repo memory while the changes are still fresh: parser load order, app-shell map, package map, and the test checklist. After that I’ll rebuild graphify and run the full verification sweep, including the new edit-book runtime test.


Edited 6 files, ran 6 commands
The implementation is at the verification stage now. I’m running the full debug build, then the focused runtime sweep: the new edit-book flow, the existing library flow, and a reader restoration sanity check so we leave this feature with both shell confidence and restore-safety covered.


Ran 3 commands
The runtime pass is green, including the new edit-book flow. I’m doing the last repo-memory step now: rebuilding graphify after the new screen/parser file and doc updates, then I’ll do a quick final worktree review and wrap this up with the real outcomes and any residual risk.


Ran 4 commands
What Changed
Implemented the EPUB-only Edit Book flow as a separate feature pass. Library cards now expose an edit action, and EditBookScreen.kt lets us update title/author, replace the cover, append chapters, and delete chapters while blocking “delete the last chapter.” The save path runs through AppNavigationOperations.kt and EpubParserEditing.kt, which rewrite the stored EPUB atomically, refresh metadata.json, and repair saved progress if the old lastChapterHref was removed.

I also added the shared edit request models in BookEditingModels.kt, routed Screen.EditBook through AppNavigation.kt, and updated LibraryCards.kt so the edit action stays in the library instead of overloading reader behavior. Custom covers are intentionally kept out of the spine so they don’t become fake readable chapters.

Verification
Added focused tests in EpubParserEditingTest.kt, AppNavigationEditProgressTest.kt, and AppNavigationEditBookFlowTest.kt. These passed along with the existing library and reader restoration coverage.

Ran:

.\gradlew.bat assembleDebug
.\gradlew.bat testDebugUnitTest
.\gradlew.bat --% -Pandroid.testInstrumentationRunnerArguments.class=com.epubreader.app.AppNavigationEditBookFlowTest,com.epubreader.app.AppNavigationLibraryFlowTest,com.epubreader.feature.reader.ReaderScreenRestorationTest connectedDebugAndroidTest
python scripts/check_graph_staleness.py --rebuild
python scripts/check_graph_staleness.py
Docs and graph artifacts were updated. The main v1 tradeoff is that added chapters append to the end, and edited books now use a safe top-level TOC rebuild rather than preserving nested TOC structure.


28 files changed
+2050
-333
Undo
app/src/main/java/com/epubreader/core/model/BookEditingModels.kt
app/src/main/java/com/epubreader/data/parser/EpubParserEditing.kt
app/src/main/java/com/epubreader/data/parser/EpubParserBooks.kt
app/src/main/java/com/epubreader/data/parser/EpubParser.kt
app/src/main/java/com/epubreader/app/AppNavigationOperations.kt
app/src/main/java/com/epubreader/feature/editbook/EditBookScreen.kt
app/src/main/java/com/epubreader/app/AppNavigationContracts.kt
app/src/main/java/com/epubreader/app/AppNavigationLibrary.kt
app/src/main/java/com/epubreader/core/ui/LibraryCards.kt
app/src/main/java/com/epubreader/MainActivity.kt
app/src/main/java/com/epubreader/app/AppNavigation.kt
app/src/main/java/com/epubreader/app/AppNavigation.kt
app/src/main/java/com/epubreader/app/AppNavigation.kt
app/src/test/java/com/epubreader/data/parser/EpubParserEditingTest.kt
app/src/test/java/com/epubreader/app/AppNavigationEditProgressTest.kt
app/src/test/java/com/epubreader/data/parser/EpubParserEditingTest.kt
app/src/main/java/com/epubreader/data/parser/EpubParserEditing.kt
app/src/main/java/com/epubreader/data/parser/EpubParserEditing.kt
app/src/test/java/com/epubreader/data/parser/EpubParserEditingTest.kt
app/src/test/java/com/epubreader/data/parser/EpubParserEditingTest.kt
app/src/androidTest/java/com/epubreader/app/AppNavigationEditBookFlowTest.kt
app/src/androidTest/java/com/epubreader/app/AppNavigationEditBookFlowTest.kt
docs/README.md
docs/package_map.md
docs/app_shell_navigation.md
docs/epub_parsing.md
docs/project_graph.md
docs/test_checklist.md
