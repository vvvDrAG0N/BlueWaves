TODO

Execution Order

1. AI AGENT IMPROVEMENT PLAN
- Improve agent performance, intelligence, persistent memory, token efficiency, task handling, and debug handling.
- Define model responsibilities and task-routing rules for Gemini and Codex.
- Evaluate multi-agent support through Microsoft Agent Framework after the single-agent workflow is cleaned up.
- Keep AI-doc hygiene aligned with the canonical runtime docs root and the `docs/agent_memory/` step-history schema.

2. Performance and stability pass
- Investigate the app size regression from 15.1 MB to 60+ MB.
- Reduce first-open and Settings lag; verify whether system bar handling contributes to the slowdown.
- Improve large-book save performance after editing.
- Improve large-book import performance and add visible import progress feedback.

3. Reader and theme UX polish
- Add system foreground color and wire it into the advanced theme system.
- Highlight the selected theme in Settings and the reader bottom bar, and make theme entry points reveal the currently applied theme.
- Adjust dialogs to use primary color for confirmation and system foreground color for cancellation.
- Fix chapter-info contrast on book covers when light reader foreground colors are active.

4. Selectable Text follow-up
- Stabilize selectable text and keep Define/Translate in the in-app WebView flow, avoiding installed-app integrations and related Android permission/package-resolution issues.

5. Theme and settings expansion
- Add support for bulk theme import via `.zip` or multi-select.
- Redesign Global Settings from a tabbed UI to a list-based flow: General, Fonts, Themes, Network (future).

6. Infinite Scroll Reader Mode
- Keep this behind the performance/stability pass because it touches high-risk reader restoration and chapter-loading behavior.
- Detailed design notes are preserved below.

7. PDF / format support later
- Keep deferred until the EPUB, editor, and reader backlog is stable.
- Detailed requirements are preserved below.

- Infinite Scroll Reader Mode -

1. Feature Overview

- Add a new Reading Mode: Infinite Scroll
- Available in:
  - Reader Settings → General
  - Global Settings → General
- Allows continuous vertical scrolling across chapters instead of manual chapter switching

---

2. Core System Design

2.1 Chapter Window Model

- Maintain a sliding window of exactly 3 chapters:
  - "[Previous, Current, Next]"
- “Current” = chapter whose center is closest to viewport center

---

2.2 Initial Load

- When opening book at chapter "N":
  - Load "[N-1, N, N+1]"
  - Scroll directly to chapter "N"
- Handle edges:
  - If "N == 0" → no previous
  - If last chapter → no next

---

2.3 Scroll-Based Loading

Scrolling Down:

- When current chapter becomes "N+1":
  - Load "N+2"
  - Unload "N-1"

Result:

Before: [N-1, N, N+1]
After:  [N, N+1, N+2]

---

Scrolling Up:

- When current chapter becomes "N-1":
  - Load "N-2"
  - Unload "N+1"

Result:

Before: [N-1, N, N+1]
After:  [N-2, N-1, N]

---

2.4 Preloading

- Trigger loading early (not at boundary):
  - At ~70–80% scroll toward next/previous chapter
- Ensure next/previous chapter is ready before entering view

---

2.5 Chapter Structure

- Each chapter must be a separate view/block
- Do NOT merge chapters into one text
- Allows:
  - independent loading/unloading
  - better performance
  - cleaner state tracking

---

3. State Management

3.1 Current Chapter Detection

- Determine current chapter by:
  - chapter whose center is closest to viewport center

---

3.2 Reading Progress

- Track progress as:
  - "chapter_index"
  - "offset_inside_chapter" (percentage or pixel)
- Do NOT rely on global scroll Y

---

3.3 Restore Position

- On reopen:
  - Load "[N-1, N, N+1]"
  - Scroll to saved offset in chapter "N"

---

4. Integration & UX

4.1 Settings Integration

- Add toggle:
  - Reading Mode:
    - Paginated (default)
    - Infinite Scroll

---

4.2 Mode Switching

- Switching modes should:
  - preserve reading position
  - convert position correctly:
    - paginated → scroll offset
    - scroll → page/chapter position

---

4.3 Visual UX

- Optional:
  - chapter divider (title or spacing)
- Ensure:
  - smooth transitions between chapters
  - no visual jump on load

---

4.4 Theme Compatibility

- Infinite scroll must respect:
  - theme colors
  - fonts
  - spacing
  - line height
- Apply dynamically to all loaded chapters

---

5. Performance Constraints

- Never keep more than 3 chapters loaded
- Load only 1 chapter at a time
- Use async loading
- Avoid blocking UI thread

---

6. Edge Cases & Stability Rules

6.1 Fast Scrolling (Up / Down)

- Do NOT assume sequential scrolling
- On fast scroll:
  - detect current chapter instantly
  - rebuild correct window "[Current-1, Current, Current+1]"
- Skip intermediate loads if necessary

---

6.2 Scroll Direction Changes

- Rapid up/down scrolling must NOT:
  - trigger duplicate loads
  - reload same chapter repeatedly
- Add debounce / load lock:
  - prevent repeated triggers in short time

---

6.3 Boundary Flickering

- Prevent rapid switching near chapter edges
- Add hysteresis:
  - chapter becomes “current” only after crossing ~55% of screen

---

6.4 Load Safety

- Do NOT load if:
  - chapter already loaded
  - chapter currently loading
- Maintain loading state per chapter

---

6.5 Unloading Safety

- Only unload after new chapter successfully loaded
- Never unload:
  - current chapter
  - visible adjacent chapter

---

6.6 Recovery Logic

- If system becomes inconsistent:
  - (missing chapter OR >3 loaded)
- Reset:
  - keep current chapter
  - reload "[Current-1, Current, Current+1]"

---

6.7 Boundary Conditions

- First chapter:
  - no previous load
- Last chapter:
  - no next load
- Prevent invalid index access

---

6.8 Load Cancellation

- If user scrolls far away:
  - cancel outdated loading tasks
- Only keep relevant loads active

---

7. Failure Prevention Rules

- Do NOT:
  - merge entire book into one view
  - load all chapters at once
  - rely only on scroll position without chapter tracking

---

8. Future Optimization (Optional)

- Increase window size dynamically for high-end devices (3 → 5)
- Add caching for previously loaded chapters
- Preload images/assets separately

---

9. Implementation Order (Important)

1. Core window system (3 chapters)
2. Scroll detection + current chapter logic
3. Load/unload logic
4. Progress tracking + restore
5. Settings toggle + mode switching
6. Edge cases & stability rules
7. Performance optimization

---

- PDF / Format Support LATER
  - Add support for PDF.
  - Revisit ZIP-based import flows only when the format/runtime story is coherent again.
  - Requirement: ship unit tests for format detection, import routing, and parser/reader entry points.
  - Requirement: update relevant markdown docs.
  - Requirement: rebuild graphify artifacts after implementation.

