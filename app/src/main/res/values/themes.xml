<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EpubReader" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.EpubReader" parent="Base.Theme.EpubReader" />

    <style name="Theme.EpubReader.Launch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenBackground">@color/launch_splash_background</item>
        <item name="postSplashScreenTheme">@style/Theme.EpubReader</item>
    </style>
</resources>
